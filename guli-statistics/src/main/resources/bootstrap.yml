spring:
  cloud:
    config:
      uri: http://localhost:8888
      label: master
    bus:
      enabled: true
      refresh:
        enabled: true
      trace:
        enabled: true
  application:
    name: statistics
  profiles:
    active: dev

  rabbitmq:
    host: 192.168.175.200
    port: 5673


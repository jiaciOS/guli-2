server:
  port: 8888


spring:
  application:
    name: guli-config
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kaimeng110091/guli.git
      label: master
